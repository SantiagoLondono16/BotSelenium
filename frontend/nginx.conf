server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — return index.html for any unknown path so React Router works
    location / {
        try_files $uri $uri/ /index.html;
    }
}
